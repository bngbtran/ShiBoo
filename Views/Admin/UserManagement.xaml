<UserControl x:Class="ShiBoo.Views.Admin.UserManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
    <TextBlock Text="QUẢN LÝ NHÂN VIÊN" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
    
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="Quản lý Ca Trực →" Click="BtnGoToShifts_Click" 
                Background="Orange" Foreground="Black" Padding="15,5" FontWeight="Bold" Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="3"/>
                </Style>
            </Button.Resources>
        </Button>
    </StackPanel>
</Grid>

        <Border Grid.Row="1" Background="#F9F9F9" Padding="15" CornerRadius="5" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Họ tên:"/>
                    <TextBox x:Name="txtName" Width="150" Margin="0,5"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Email:"/>
                    <TextBox x:Name="txtEmail" Width="180" Margin="0,5"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Chức vụ:"/>
                    <ComboBox x:Name="cbRole" Width="100" Margin="0,5">
                        <ComboBoxItem Content="Member" IsSelected="True"/>
                        <ComboBoxItem Content="Admin"/>
                    </ComboBox>
                </StackPanel>
                <Button Content="Thêm Nhân Viên" Click="BtnAdd_Click" VerticalAlignment="Bottom" 
                        Padding="15,5" Background="DarkGreen" Foreground="White" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgUsers" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Họ Tên" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Vai Trò" Binding="{Binding Role}" Width="100"/>
                <DataGridTemplateColumn Header="Thao tác" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Xóa" Click="BtnDelete_Click" Background="Crimson" Foreground="White" Padding="5,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>